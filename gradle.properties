#Kotlin
kotlin.code.style=official
kotlin.daemon.jvmargs=-Xmx2048M
kotlin.suppressGradlePluginWarnings=DeprecatedGradleVersionWarning

#Gradle
org.gradle.jvmargs=-Xmx2048M -Dfile.encoding=UTF-8
org.gradle.configuration-cache=false
org.gradle.caching=true
version=0.4.0

# JDK: no installations.paths override — the foojay-resolver-convention plugin (settings.gradle.kts)
# auto-downloads the JETBRAINS-vendor JDK 21 the toolchain requests (build.gradle.kts) on any OS.
# A hardcoded Windows path here previously broke `./gradlew` on Linux/macOS.
